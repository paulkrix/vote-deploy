apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: vote-ui
  template:
    metadata:
      labels:
        app: vote-ui
    spec:
      containers:
      - image: docker.io/paulkrix/vote:vfc8bc39e6beedbc432b3fea0fec7939538972048
        name: vote-ui
        ports:
        - containerPort: 80
